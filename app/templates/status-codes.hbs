{{list-filter list=this attributes='code, title, summary'
              strictMatch=false
              listClass='list--unstyled'
              autofocus=true
              placeholder='Search for a status code...'}}

{{outlet}}
